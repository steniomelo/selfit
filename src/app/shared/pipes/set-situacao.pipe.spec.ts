import { SetSituacaoPipe } from './set-situacao.pipe';

describe('SetSituacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new SetSituacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
